# devtoolsUI

1. Go to src folder
`cd portal.ui/src/`

2. Now install npm packages
`npm install`

3. Run build
`npm run build`

4. start server
`npm start`

5. Open localhost:3000 on browser and play around.
