import { Component, OnInit, ViewEncapsulation } from '@angular/core';
import { Validators, FormControl, FormGroup, FormBuilder, REACTIVE_FORM_DIRECTIVES, AbstractControl, Control } from '@angular/forms';
import { Router,NavigationEnd } from '@angular/router';
import { Headers } from '@angular/http';


@Component({
	templateUrl: 'app/views/bankValidator.html'
	selector: 'bankValidator'
})

export class BankValidatorComponent implements OnInit{


}